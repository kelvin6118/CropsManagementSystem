import { View, Text } from 'react-native'
import React from 'react'

const CropsForm = (navigation) => {
  return (
    <View>
      <Text>CropsForm</Text>
    </View>
  )
}

export default CropsForm
